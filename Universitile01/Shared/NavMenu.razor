<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="">Uni-Versitile</a>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-0">
            <NavLink class="nav-link" href="dashboard" >
                <span class="oi oi-monitor" aria-hidden="true"></span> Dashboard
            </NavLink>
        </div>
        <div class="nav-item px-0">
            <NavLink class="nav-link" href="attendance">
                <span class="oi oi-person" aria-hidden="true"></span> Personal Info
            </NavLink>
        </div>
        <div class="nav-item px-0">
            <NavLink class="nav-link" href="">
                <span class="oi oi-book" aria-hidden="true"></span> Supporting Material
            </NavLink>
        </div>
        <div class="nav-item px-0">
            <NavLink class="nav-link" href="Assignments">
                <span class="oi oi-list" aria-hidden="true"></span> Assignments
            </NavLink>
        </div>
        <div class="nav-item px-0">
            <NavLink class="nav-link" href="Quizes&Tests">
                <span class="oi oi-puzzle-piece" aria-hidden="true"></span> Quizes/Tests
            </NavLink>
        </div>
    </nav>
</div>

@code {
    // This variable controls whether the navigation menu is collapsed or not
    private bool collapseNavMenu = true;

    // This property returns the CSS class for the navigation menu based on whether it is collapsed or not
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    // This method toggles the value of the collapseNavMenu variable to show/hide the navigation menu
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
